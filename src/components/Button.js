import React from 'react';
// import './fonts.css';
import './Button.css';

const Button = (props) => {
 
 
 
 
 
    return (
    <button className={classList}>
        {props.label}
    </button> )
}

export default Button; 
